spring:
  application:
    name: order-service
  cloud:
    config:
      enabled: true
      discovery:
        enabled: true
        service-id: CONFIG
eureka:
  instance:
    non-secure-port: 9703
  client:
    service-url:
      defaultZone: http://${eureka.host:localhost}:${eureka.port:9700}/eureka/